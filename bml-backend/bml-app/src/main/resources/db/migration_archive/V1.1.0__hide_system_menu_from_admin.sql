-- ============================================================================
-- V1.1.0 — 中台管理平台隐藏"系统管理"菜单
-- ============================================================================
-- 系统管理功能已迁移至前台业务系统（ip:port/），中台管理平台（ip:port/admin）不再
-- 显示该目录及其子菜单。设置 visible=0 使其在路由构建时标记为 hidden，不影响权限标识
-- 的正常解析。
--
-- 受影响菜单：
--   id=4   系统管理（目录）
--   id=41  用户管理
--   id=42  角色管理
--   id=43  菜单管理
--   id=44  部门管理
-- ============================================================================

UPDATE sys_menu SET visible = 0 WHERE id = 4;
UPDATE sys_menu SET visible = 0 WHERE parent_id = 4;
